# next steps --> potential additions/enhancements

a) Persist to SQLite/PostgreSQL via SQLAlchemy

b) JWT-based user authentication

c) Recurring-event support (e.g. “every Mon/Wed/Fri”)

d) Conflict checks (prevent overlapping assignments)

e) A React or Vue front end that visualizes the calendar grid.
